import { future as baseTheme } from '@mdx-deck/themes'

export const summit = {
    ...baseTheme,
    prism: {
        graphQL: {
            fontSize: '12px',   
        }
    },
    code: { fontSize: '12px' },
    pre: { fontSize: '12px' },
}